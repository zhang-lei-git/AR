import * as THREE from './assets/vendor/three.module.min.js';
const container=document.getElementById('twinThreeViewer');let renderer,scene,camera,lineGroup;const stations=[];
function mat(color,metal=.58,rough=.38){return new THREE.MeshStandardMaterial({color,metalness:metal,roughness:rough})}function mesh(geometry,material,pos,rot=[0,0,0]){const m=new THREE.Mesh(geometry,material);m.position.set(...pos);m.rotation.set(...rot);m.castShadow=true;m.receiveShadow=true;return m}function box(size,pos,material,rot){return mesh(new THREE.BoxGeometry(...size),material,pos,rot)}function cyl(r,len,pos,material,rot=[0,0,0]){return mesh(new THREE.CylinderGeometry(r,r,len,24),material,pos,rot)}
function support(index){const g=new THREE.Group(),yellow=mat(0xc68b18),yellowDark=mat(0x8c5c10),red=mat(0x8c3028),chrome=mat(0xd8dddf,.9,.15),black=mat(0x242a2c,.25,.75);g.add(box([3.5,.34,1.65],[0,-.82,0],yellow));g.add(box([3.9,.32,1.95],[0,1.45,.25],yellow,[.04,0,0]));g.add(box([2.8,.25,1.45],[0,.42,-.48],yellowDark,[-.38,0,0]));[-.9,.9].forEach(x=>{g.add(cyl(.22,2.05,[x,.18,.22],red));g.add(cyl(.14,1.15,[x,1.02,.22],chrome))});g.add(box([2.45,.22,.82],[0,-.15,-.55],black));g.scale.set(.65,.65,.65);g.position.set(index*3.8-5.7,0,0);g.userData.materials=[yellow,yellowDark,red,chrome,black];return g}
function build(){scene=new THREE.Scene();scene.background=new THREE.Color(0x15191b);scene.fog=new THREE.Fog(0x15191b,14,30);camera=new THREE.PerspectiveCamera(33,1,.1,100);camera.position.set(10,7.5,13);camera.lookAt(0,0,0);renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFShadowMap;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.12;container.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xe1e7e8,0x161a1c,1.7));const key=new THREE.DirectionalLight(0xfff2d8,4);key.position.set(5,9,7);key.castShadow=true;scene.add(key);const fill=new THREE.DirectionalLight(0x91bfd3,1.4);fill.position.set(-6,3,-4);scene.add(fill);lineGroup=new THREE.Group();lineGroup.add(box([16,.22,4],[0,-1.28,0],mat(0x343a3d,.1,.86)));for(let i=0;i<4;i++){const group=support(i);stations.push(group);lineGroup.add(group);lineGroup.add(box([3.25,.08,2.75],[i*3.8-5.7,-1.08,0],mat(0x555d60,.18,.72)))}scene.add(lineGroup);const floor=box([24,.12,16],[0,-1.48,0],mat(0x262b2d,.05,.95));scene.add(floor);const grid=new THREE.GridHelper(22,44,0x5c6264,0x34393b);grid.position.y=-1.4;scene.add(grid);const resize=()=>{const w=Math.max(1,container.clientWidth),h=Math.max(1,container.clientHeight);renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix()};new ResizeObserver(resize).observe(container);resize();(function animate(){lineGroup.rotation.y+=.00055;renderer.render(scene,camera);requestAnimationFrame(animate)})()}
function update(objects,selected){stations.forEach((g,i)=>g.traverse(o=>{if(!o.material)return;const active=i===selected,alert=objects[i]?.alert;o.material.emissive?.setHex(active?0x553200:alert?0x3b1600:0);o.material.emissiveIntensity=active ? .65 : alert ? .45 : 0}))}window.addEventListener('twin-data-loaded',e=>update(e.detail.objects,e.detail.selected));build();
